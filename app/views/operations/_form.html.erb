<%= simple_form_for operation do |f| %>
  <%= f.input :name %>
  <%= f.input :amount %>
  <%= f.input :kind, collection: ['Alimentation', 'Charges', 'Transport', 'Santé', 'Restaurant', 'Loisirs', 'Cadeaux', 'Autre'], include_blank: false, as: :select %>
  <%= f.button :submit, class: 'btn btn-info' %>
<% end %>
